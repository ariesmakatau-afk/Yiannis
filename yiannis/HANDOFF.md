# Handoff Notes — Yianni's on Hindley Street

Picking this up in a fresh Claude conversation? Paste this whole file in as context,
or just say "read HANDOFF.md" once the project is uploaded there.

## Setup

```bash
npm install
cp .env.example .env.local   # fill in real values — see README.md
npm run dev
```

## What changed in this round

1. **Address confirmed** — `business.address.confirmed` is now `true` in `lib/content.ts`.
2. **Sauces list finalized** — 11 sauces in `lib/content.ts` (`sauces` category), 50c each:
   Garlic, Mustard, Peri Peri, Nando's Peri-Peri, Aioli, Mayonnaise, Hot Chilli, Sweet Chilli,
   Tomato, BBQ, Tabasco.
3. **Full drinks menu added** — split into 5 new categories in `lib/content.ts`:
   `drinks-soft`, `drinks-powerade`, `drinks-water`, `drinks-juice-milk`, `drinks-cans`
   (plus the original `drinks` category, which just has Greek Coffee). All new items are
   priced `"Price on request"` — **search `lib/content.ts` for "Price on request" and replace
   with real prices** once the client provides them, then you can retire the note in
   `menuPlaceholders.drinks`. All 5 new categories are already wired into `/menu` (display)
   and `/order` (pickup builder) via `orderableCategoryIds` in `lib/content.ts` — no other
   code changes needed once prices are in.
4. **Logo integrated.** The client supplied a photo-style mockup (`Spartan warrior` emblem,
   text "YIANNIS / HELLANIC YIROS") with a marble-and-vignette background — not a usable
   web asset as-is. I isolated the artwork by masking on hue (navy ink vs. neutral grey/black
   backdrop) and produced clean transparent PNGs, saved to `public/images/`:
   - `logo-navy.png` / `logo-white.png` — full lockup (icon + wordmark), 1212×797
   - `icon-navy.png` / `icon-white.png` — icon only (no wordmark), 815×478
   - `logo-navy-icon-512.png` — a downscaled navy lockup, spare/unused
   - `app/icon.png` — square favicon generated from the icon mark on a navy circle

   **Flag for the client:** the logo's subtitle reads "HELLANIC YIROS" — almost certainly
   meant to be "Hellenic." Wasn't changed since it's their supplied artwork; check with them
   before it goes live everywhere. If they send a corrected or vector (SVG/AI/EPS) version of
   the logo, redo this asset pass from that instead of the current raster extraction — it'll
   look sharper at large sizes (e.g. the About page lockup) than the current raster masks do.

   Used in: `components/Header.tsx` (icon-white, compact), `components/Footer.tsx`
   (logo-white, full lockup), `app/about/page.tsx` (logo-navy, full lockup), `app/page.tsx`
   hero (icon-white as a low-opacity watermark), `app/icon.png` (favicon).

5. **Color palette matched to the logo** — sampled the logo's navy directly from the
   artwork and updated `tailwind.config.ts`: `cobalt.DEFAULT` is now `#0d3b6f` (was
   `#1747a6`). `cobalt.light` / `cobalt.dark` were adjusted to match.
6. **Greek meander (key pattern) theme extended.** There was already a `.meander-divider`
   utility in `app/globals.css` (white, used once in the footer). Added a
   `.meander-divider-navy` variant and placed small accent dividers under the page `<h1>` on
   About/Menu/Location/Contact, and above "Why Yianni's" on the homepage, to carry the motif
   (referenced from the client's Canva mood image of 3D Greek columns/marble) through more of
   the site without it feeling like a one-off footer decoration.

## Update — drink prices added

All drink prices are now filled in `lib/content.ts` (client-supplied, confirmed 16 Sep 2026):
- Soft drinks (600ml): $5.00
- Powerade: $5.50
- Water (Mount Franklin, still/sparkling/sparkling lime): $4.00
- Pump / Pump Berry: $5.50
- Nippy's juices, milk & iced coffee: $5.00
- Cans (330ml): $3.50

Also added a new `drinks-energy` category (Monster Energy White Zero, Monster Energy Mango
Loco — $5.50 each), wired into `orderableCategoryIds` so it shows on `/menu` and `/order`
automatically.

The old "prices to follow" placeholder note and the now-unused `menuPlaceholders` /
`drinksCategoryIds` exports have been removed from `lib/content.ts`, and `app/menu/page.tsx`
no longer references them. **Drink pricing is no longer a blocker.**

## Update — "HELLENIC" spelling corrected

The client's original logo artwork read "HELLANIC YIROS." Since no corrected source file was
supplied, I fixed it directly in the existing raster assets: the "A" in "HELLANIC" was
replaced with the "E" glyph copied from elsewhere in the same wordmark (same font, weight,
color, baseline — pixel-perfect match, no font-guessing involved), then centered in place.
Fixed files: `public/images/logo-navy.png`, `public/images/logo-white.png`, and the spare
`public/images/logo-navy-icon-512.png` (regenerated by downscaling the fixed navy lockup).
`icon-navy.png` / `icon-white.png` / `app/icon.png` never contained the wordmark text, so
they were untouched.

**If the client later sends a proper corrected or vector logo file, redo this asset pass from
that instead** — it'll be sharper at large sizes than this raster patch, especially for the
About page's full-size lockup.

## Update — lighter theme + Greek-inspired type system

Reworked the visual language per client direction: "blue and white, but lighter, chill vibes"
plus real Greek-style calligraphy instead of the plain sans-serif.

- **Palette softened** (`tailwind.config.ts`): the old strong navy (#0d3b6f/#082746) is now a
  "harbor blue" (`cobalt` = #2f5578 / light #6d93b4 / dark #1c3850) — still clearly "the blue,"
  much less heavy. Contrast-checked against white text (7.8:1 DEFAULT, 12:1 dark — both pass
  WCAG AAA). Added `mist` (#eef3f5, pale blue-grey) for alternating section backgrounds instead
  of flat white/cobalt-tint, and `sand` (#faf8f4, warm off-white) as the new body background —
  the whole site no longer sits on stark white.
- **Typography** (`app/layout.tsx`): swapped Space Grotesk for **Cinzel** (a serif carved from
  actual Roman/Greek inscriptional lettering) as the display/heading font, and added
  **Cormorant Garamond italic** as a script accent font (`font-script`) for small taglines —
  e.g. "Est. on Hindley Street" over the hero, "Yamas!" over the final CTA. Body copy stays on
  Inter for readability.
- **New decorative motif**: `public/images/marble-scene.svg` — an original, hand-built SVG
  (soft blue-grey gradient, one large "orb," abstracted column line-art, a meander trim strip)
  used via the new `.scene-bg` utility class behind the hero and the two navy call-to-action
  sections on the homepage. This is intentionally *not* a copy of any reference/mood-board
  image — the client's references included another studio's Canva stock asset and two other
  restaurants' branding (PHEAS, NOMA), which aren't ours to reuse on a paying client's site, so
  the mood was rebuilt from scratch as original artwork instead.
- Logo files were **not** recolored — they stay the bold "signature" navy from the brand mark,
  which now reads as an intentional deeper accent against the lighter site chrome.

Because the whole codebase already used semantic classes (`font-display`, `bg-cobalt`,
`text-cobalt`) rather than one-off hex values, this change cascades through every page
automatically — About/Location/Contact/Order all pick up the new palette and type without
being touched individually. Homepage got extra hand-styling (script taglines, the marble scene
on hero + two CTA blocks) since it's the highest-visibility page.

**Next up, by design:** the actual `/menu` page layout and the `/order` builder UI haven't been
redesigned yet — they're functionally intact but visually still plain. That's the deliberately
separate next step (menu content/pricing was already finished in the previous pass; now it's a
pure visual pass on the same new design system).

## Still outstanding (unchanged from before, plus new items)

- [ ] **Consider a vector version of the logo** — current assets are raster (PNG) extractions
      from a mockup image; fine for web at current sizes, but ask the client for an SVG/AI
      source file if one exists, for future-proofing (large print use, very large hero
      placements, etc.)
- [ ] Domain name — still unset (`PLACEHOLDER.domain` in `lib/content.ts`)
- [ ] Food & interior photography — still grey placeholders (`PlaceholderImage.tsx`)
- [ ] Google Business Profile link
- [ ] Featured reviews on homepage — still placeholders
- [ ] Privacy Policy / Terms real copy
- [ ] Real embedded map (Home / Location / Contact)
- [ ] GA4 property ID (`NEXT_PUBLIC_GA_ID`)
- [ ] The meander accent hasn't been added to the Order page or between menu categories yet
      — worth a pass if you want the theme fully consistent everywhere, not just page headers.

## Notes

- No `npm install` / build was run on this end (sandboxed, no network access) — worth a
  quick `npm run build` locally before deploying again, just to confirm the new Image
  imports and the split drinks categories compile clean.
- Ordering system: pickup path (`/order`) and Uber Eats link are still deliberately separate
  — don't merge them (see README.md).

## Update — new medallion logo, marble theme, Fraunces + Sora type

Client supplied two new images this round: a **circular marble medallion logo** (Spartan
relief, Greek-key border, "YIANNIS / HELLENIC YIROS" — spelling now correct in the artwork
itself) and an **architectural marble still-life** (plaster wall, fluted columns, marble
spheres, a carved rosette) to drive the site's visual theme.

### Logo
- Background removed: the medallion was isolated from its marble-slab backdrop with a
  feathered circular mask, cropped tight. Saved as `public/images/medallion.png` (1024px
  master), `medallion-512.png` (in-page use) and `medallion-160.png` (small/spare).
- **The old header-logo distortion is fixed.** The previous header squeezed the 815×478
  *wide* `icon-white.png` into an `h-10 w-10` *square* box, which is what made it look
  mushed. The new mark is square, sits in a square box, and the header is now 72px tall with
  proper spacing and a `truncate`-safe two-line wordmark.
- The medallion now appears in the header, footer, About page, `/menu` (as a faint
  watermark), the hero (as the centrepiece) and `app/icon.png` (favicon, on brand blue).
- **Superseded assets deleted**: `icon-navy.png`, `icon-white.png`, `logo-navy.png`,
  `logo-white.png`, `logo-navy-icon-512.png`, and the previous hand-built `marble-scene.svg`.
  All references were updated — nothing points at them anymore.

### Typography — Fraunces + Sora
`app/layout.tsx` now loads **Fraunces** (display/headings + italic for taglines) and **Sora**
(body, nav, UI). Both are variable fonts, so no fixed `weight` arrays are passed — only
`axes: ["SOFT", "WONK"]` on Fraunces for its characteristic soft wonk. Cinzel, Cormorant
Garamond and Inter are gone. `h1–h4` switched from `tracking-wide` to `tracking-tight`, since
Fraunces has enough character of its own and doesn't want letterspacing.

### Theme — the still-life, used as a design system (not pasted in)
The reference image was **decomposed into reusable parts** rather than dropped in as a
background:

| Asset | Source | Used as |
|---|---|---|
| `texture-marble.jpg` | clean top-left wall texture | tiling ground for `.marble-surface` / `.marble-wash` |
| `scene-feature.png` | full columns + rosette composition, edge-feathered | `.scene-bg` atmosphere behind hero and the two blue CTA bands |
| `column-left.png`, `column-right.png` | the two fluted/doric columns, feathered | flank and visually "support" the shopfront image on the homepage About block |

New utility classes in `app/globals.css`:
- `.marble-surface` / `.marble-wash` — textured section grounds (replaces flat `bg-mist`)
- `.scene-bg` — the still-life as atmosphere
- `.plinth` — a marble ledge-and-shadow that objects stand on; used under each
  "What's Cooking" tile and the shopfront image, echoing the reference's marble shelf
- `.btn-orb` / `.btn-orb-blue` — **the marble spheres reinterpreted as buttons**: fully round
  pills with a radial gradient lit from the top-left (matching the reference's light
  direction) plus inner and drop shadows. `btn-orb` is the pale marble one, `btn-orb-blue`
  the brand-blue primary. These replaced `btn-primary` / `btn-secondary` /
  `btn-outline-dark` sitewide.
- `.carved-panel` — bas-relief card: light top edge, soft inset + drop shadow, as if recessed
  into stone. Used for menu categories, review cards, the map placeholder and OrderFlow panels.
- `.menu-panel` / `.menu-panel-link` — **the themed dropdown**: the mobile menu is now a
  translucent marble panel with a Greek-key trim strip along its top edge, large Fraunces
  labels with arrow affordances, and an orb CTA at the bottom. The same `.menu-panel`
  treatment is reused for the `/menu` sticky category bar and the mobile bottom nav.

Header also flipped from solid blue to a translucent marble/sand bar, so the blue is now an
accent against the marble rather than the site's dominant colour — closer to the reference's
light, stone-forward feel. Desktop nav breakpoint moved `md` → `lg` to give the longer
Fraunces wordmark room before it collapses.

### Notes / next steps
- Images were optimized on the way in (medallion master downscaled 1802→1024, texture stored
  as JPG); `public/images` totals ~3.1MB. `scene-feature.png` is the biggest single file at
  ~1MB — if Lighthouse complains, convert it to WebP, it's purely decorative.
- **Still not run here:** `npm install` / `npm run build` (sandboxed, no network). The
  Fraunces/Sora swap and the new `next/font` config are the most likely things to surface a
  build error, so build locally before deploying.
- `/order` builder got the panel/orb treatment but **not** a full layout redesign — that's
  still the open visual task, same as noted in the previous round.

## Update — first real food photos added (homepage)

Client-supplied phone photos processed (cropped to the right aspect ratios, light contrast/
colour/sharpness pass) and wired into `app/page.tsx`:

- **"What's Cooking" tiles** (all 4, was `PlaceholderImage`): `food-yiros.jpg`,
  `food-platters.jpg`, `food-chips.jpg`, `food-drinks.jpg` in `public/images/`.
- **Homepage "Interior" plinth image** (was `PlaceholderImage`, aspect="wide"):
  `interior-wide.jpg` — the charcoal rotisserie + fresh wrap shot, cropped to 21:9.
- `PlaceholderImage` import removed from `app/page.tsx` (no longer used there).

Two customer-facing/identifiable photos from this batch were deliberately **not** used
anywhere on the site: one shows a customer's face (client declined to use it), the other was
a generic stock-looking flatbread photo (not the client's own product shot).

## Update — storefront photo added (About page)

Client supplied a daylight exterior shot of 270 Hindley St. Cropped to `aspect-[21/9]`,
light contrast/colour/sharpness pass, saved as `public/images/storefront-wide.jpg`, wired
into `app/about/page.tsx`. `PlaceholderImage` import removed from that file — the component
itself is still in `components/PlaceholderImage.tsx`, just unused for now, in case a future
photo gap needs it.

**All six original photo placeholders are now filled.** A second candidate storefront photo
(an old 2018 Facebook screenshot, low-res with app UI baked in and a crowd of identifiable
faces at night) was not used — the daylight shot was the clear pick.

## Update — featured reviews added (homepage)

Replaced the "What Adelaide Says" placeholder cards in `app/page.tsx` with three real,
client-confirmed reviews (`featuredReviews` array): **brianhissy** (Tripadvisor, Apr 2022),
**David Maddison** (Feb 2025), **Andrew Jones** (Jan 2025) — quotes lightly trimmed for
length, names/dates as supplied by the client. All shown as 5-star. The "Read reviews on
Facebook" link below the cards was retitled "Read more reviews" since the reviews span more
than one platform (Tripadvisor + Google).

**All homepage/About placeholders (photos and reviews) are now filled.** Remaining open
items are unchanged from the rest of this file (domain name, real embedded map, GA4 property
ID, Privacy/Terms copy).

## Update — Privacy Policy, Terms, and map embeds filled in

**Privacy Policy & Terms** (`app/privacy/page.tsx`, `app/terms/page.tsx`): replaced the
`[PLACEHOLDER]` boxes with real, plain-language copy grounded in what the site actually does
— reading straight from `lib/notifyOrder.ts` (pickup form collects name/phone/pickup
time/items, sent via Telegram or SMS, pay-in-store only, no online payment) and
`lib/analytics.ts` (optional GA4 usage tracking, no-ops if `NEXT_PUBLIC_GA_ID` is unset).
Both pages end with a small note flagging that this is plain-language copy, not
solicitor-reviewed legal text — worth a quick legal check before launch, and definitely a
re-check if online payment or a loyalty program is added later.

**Map placeholders** (homepage, `/location`, `/contact`): all three replaced with a new
shared `components/MapEmbed.tsx` — a keyless Google Maps iframe (`google.com/maps?q=...&output=embed`),
no API key or billing required. If the client later gets a Google Maps API key and wants
richer styling/markers, swap the iframe `src` for the proper Maps Embed API URL
(`google.com/maps/embed/v1/place`) inside that one component — all three pages update
automatically.

**Still outstanding:** domain name (`PLACEHOLDER.domain` in `lib/content.ts`), GA4 property
ID (`NEXT_PUBLIC_GA_ID` — see README/`.env.example`), and confirming an SMS/Telegram provider
for order notifications (`lib/notifyOrder.ts`).

## Update — menu rebuilt as an option-driven system + full copy rewrite

### New menu model (`lib/menu.ts`)
The flat category/item list in `lib/content.ts` is **superseded** (left in place,
clearly marked, nothing imports it — it preserves the client's original confirmed
price list for cross-checking). Both `/menu` and `/order` now read `lib/menu.ts`.

Structure is option-driven, not one-line-per-permutation. A customer picks a
product, then meat / size / extras:

- **Meat** is a multi-select: tap one for a single meat, or two or three for a
  **Mix**. Lamb carries a **+$2** surcharge, applied once if lamb is in the
  selection. Groups: Yiros, Yiros Pack, AB Pack, Meat Pack, Platters (dine-in
  only), Sides & Sauce, Coffee, Drinks.
- **Extras rules live in the data** (`allowedExtras` per product), so the
  restrictions can't drift out of sync with the UI:
  - Yiros → cooked onion, chips, cheese, extra meat (all four)
  - Yiros Pack → **no chips**
  - AB Pack → **no chips** (it already comes on chips)
  - Meat Pack → **no extra meat**
  - Platters → cooked onion, cheese
- **Drinks are grouped, not listed flat**: five products (Soft Drink, Can, Water,
  Juice/Milk/Iced Coffee, Sports & Energy) each with a flavour sub-select, plus
  Greek Coffee on its own. This replaces the seven separate drink categories.

### Order builder (`components/OrderFlow.tsx`, `components/ProductCustomiser.tsx`)
The "Add" button problem is fixed. Tapping a product now opens a customiser sheet
(slides up on mobile, fades in on desktop) with meat/size/variant/extras chips, a
**quantity stepper**, and a live running price in the button itself
("Add to order · $44"). After adding: the product card flashes and switches to
"✓ Added to your order" for ~1.6s, and the cart badge bumps. The summary panel
shows each line's full configuration, per-line quantity controls, and an estimated
total. All animations respect `prefers-reduced-motion`.
New CSS in `globals.css`: `.chip-option`, `.qty-btn`, `.customiser-sheet`,
`.added-flash`, `.cart-bump` + keyframes.

### Copy rewrite
Rewrote `heroCopy`, `aboutCopy`, `whyYiannis`, `locationIdentifier`, `siteMeta` in
`lib/content.ts`, plus the homepage section headings and the menu page intro. New
angle: a long-standing institution with a deliberately short menu — "three meats,
one fire" — leaning on longevity and charcoal rather than generic "authentic
Greek flavours / fresh ingredients" filler. Removed the `Est. on Hindley Street`
script line from the hero.

### ⚠️ Three things needing client confirmation
1. **Lamb surcharge is now a flat +$2 everywhere**, per the client's instruction.
   The original price list had **+$3 on the large Meat Pack and Platter for 1, and
   +$4 on Platter for 2**. If those higher surcharges still apply, `meats[].surcharge`
   needs to become per-product instead of global.
2. **Dropped items.** The new structure omits **Veggie Roll ($8), Falafel Yiros ($15),
   Salad Pack ($5), Pita Bread (+$2) and Falafel (+$2)** — they weren't in the
   client's list of what they serve. The veggie roll and falafel yiros were the only
   vegetarian options on the menu, so this is worth an explicit yes/no before launch.
3. **"Decades" vs a hard number.** Copy now says "decades" / "generations" rather
   than "30 years", because the sources conflict: `business.history` says ~30 years,
   the shopfront sign says EST-2002 (~24 years), and a Google review says 25. Worth
   pinning down the real founding year — it's good SEO and good copy.

## Update — menu corrections, real history, slimmer bottom bar

### All items restored
Nothing is dropped from the original price list any more. **Falafel Yiros ($15)**,
**Veggie Roll ($8)**, **Salad Pack ($5)**, **Pita Bread ($2)** and **Extra Sauce
($0.50, any of 11 sauces)** are all back as products. Per the client, the Falafel
Yiros and Veggie Roll can both take **chips and cooked onion** as extras (plus
cheese and falafel). 18 products across 8 groups.

### Lamb surcharge is now per-size, not flat
`lambSurcharge` moved from the meat to `ProductSize`, because the premium genuinely
varies. Confirmed by the client:

| Item | Lamb premium |
|---|---|
| Yiros (regular & mini) | +$2 |
| Yiros Pack | +$2 |
| AB Pack (small & large) | +$2 |
| Meat Pack — small | +$2 |
| Meat Pack — large | +$3 |
| Platter for 1 | +$3 |
| Platter for 2 | +$4 |

The customiser reads the premium off the *selected size*, so the chip label updates
live when you switch size. Applied once if lamb is anywhere in a mix.

### Drink prices confirmed
600ml Coke-range bottles $5 · cans (330ml) $3.50 · Nippy's juices/milk/iced coffee $5 ·
Mount Franklin water $4 · Pump, Powerade & energy drinks $5.50 · Greek coffee $3.

### History copy now matches reality
A yiros shop has been on the site ~40 years; it was a *different* shop before Yianni
took it over and rebranded around 2002. Exact dates aren't documented, which is why
the copy leans on "four decades" and "old enough that it has stopped keeping count"
rather than a precise founding year. `business.history` records the full nuance for
whoever edits next. **The earlier flag about conflicting dates is resolved** — the
shopfront's EST-2002 refers to Yianni's rebrand, not the site's first yiros shop.

### Bottom bar reworked (`components/BottomNav.tsx`)
Was a full-width fixed bar permanently occupying ~64px of every mobile screen. Now a
slim floating pill (`inset-x-3 bottom-3`, rounded, ~46px) that:
- **auto-hides on scroll down** and slides back on scroll up, so reading gets the
  full viewport;
- always reappears near the top of the page and at the very bottom;
- has a chevron to dismiss it outright, leaving a small tab at the bottom-right to
  bring it back;
- ignores <6px scroll jitter so it doesn't flicker.

`main` padding dropped from `pb-16` to `pb-4`; the bottom clearance moved onto the
footer's last line (`pb-16 md:pb-0`) so only the end of the document reserves room
rather than every page.

## Update — sauce allowances + design theme rebuilt

### Sauce rules (`freeSauces` per product)
Two sauces included on every food item, **three on an AB Pack**, then
**$0.50 per additional sauce**. The allowance lives on the product
(`freeSauces`), and `sauceCost(selected, free)` does the maths. The
customiser shows a sauce picker: chips past the allowance pick up a visible
"+$0.50" badge and a running line explains the overage. The old standalone
"Extra Sauce" product was removed — sauce choice is now part of each item,
so a separate line item would have been a second, contradictory way to buy
the same thing. (Garlic Sauce *tubs* remain a product; that's a different
thing.)

### Design: the still-life is now a theme, not a pasted picture
**What was wrong.** `.scene-bg` set the whole reference composition as
`background-image: cover` behind the hero, the "Why" band *and* the final
CTA — the same photograph appearing three times down one page. Worse,
`column-left.png` / `column-right.png` were **crops of that same photo**,
carrying their own marble wall and drop shadow, so they could only ever be
pasted beside content, never actually frame it.

**What replaced it.**
- **`components/ornament/Column.tsx`** — a fluted Doric column drawn as
  vector (capital, abacus, echinus, necking rings, flutes, torus, plinth).
  It uses `currentColor` and gradient opacity, so it tints to any section
  and composites at any opacity with no baked-in background.
- **`components/ornament/Rosette.tsx`** — the carved medallion redrawn as
  vector, for 6–7% background watermarks.
- **`components/ornament/TempleFrame.tsx`** — **the pillars now hold
  something.** Two columns carry a lintel with content standing between
  them, on a stylobate. Used for the hero (columns carrying the name), the
  homepage About image and the About-page storefront. Columns hide below
  `sm` where they'd only steal width from the content they frame.
- **`.scene-atmosphere`** — the photo survives only as a 9% wash with a
  radial mask so it fades at the edges (16% + `soft-light` on cobalt
  sections). Depth, not decoration.
- `.marble-wash` is retired from the pages; `.plinth` and `.carved-panel`
  are kept — those were already doing real work.

### Colour distribution
The palette was blue-and-white only, so every accent had to be the same
cobalt — which is why it read flat and repetitive. Added **`brass`**
(`#a8873f`), taken from the Spartan helmets on the actual shopfront sign,
as a third colour: section eyebrows (`.eyebrow-brass`), header rules
(`.meander-divider-brass`, now used on every page header), review stars,
and the "Yamas!" line. Section rhythm is now deliberate and alternating:
`sand → mist → sand → cobalt-dark → mist → sand → mist → cobalt`, instead
of near-identical marble bands throughout.

### Greek humour — `shopStats` + `components/StatsBand.tsx`
Replaces the five near-identical "Why Yianni's" bullets that were padding
the deep-blue band. Four deadpan figures in a colonnade (vector columns as
the dividers): **3** meats total ("There is no fourth. People ask."),
**0** recipe changes ("It was right the first time."), **40-ish** years on
this corner ("Nobody kept the paperwork. The fire kept going."), **0%**
garlic restraint ("You will know. Tomorrow, your colleagues will know.").
The "Why it's lasted" points survive as a quieter 3-up list on the pale
band, so the page isn't repeating the same card treatment.

**Still unverified:** no `npm run build` was possible in this container
(no network for `npm install`). Imports were checked against actual exports
and syntax balance verified programmatically, but run a build before deploy.
